#ifndef MAP_H
#define MAP_H
#include "ball.h"
#include "block.h"
#include <mainwindow.h>

class Map
{
    Map(Ball ball, Block block)
        :ball(ball), block(block)
    {}

public:
    void MainWindow::pain
    {

    }

private:
    const int size;
    Ball ball;
    Block block;
};



#endif // MAP_H
